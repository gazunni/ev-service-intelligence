{
  "name": "ev-service-intelligence",
  "version": "1.0.0",
  "description": "EV Service Intelligence — recalls, TSBs, PIPs, community issues",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=8080",
    "build": "echo 'No build required for static site'",
    "deploy": "echo 'Deploy handled by Netlify'"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
